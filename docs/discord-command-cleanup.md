# Discord command cleanup

Live inventory before cleanup: 64 global Hermes commands and 14 guild commands.

## Removed

`/agents`, `/apply`, `/approvals`, `/approve`, `/background`, `/besttime`, `/blueprint`, `/branch`, `/bundles`, `/codex-runtime`, `/commands`, `/compress`, `/context`, `/curator`, `/debug`, `/deny`, `/diff`, `/egress`, `/fast`, `/footer`, `/goal`, `/heartbeat`, `/help`, `/init`, `/insights`, `/kanban`, `/learn`, `/loop`, `/memory`, `/moa`, `/model`, `/new`, `/pause`, `/personality`, `/profile`, `/queue`, `/reasoning`, `/refine`, `/reload-mcp`, `/reload-skills`, `/reset`, `/restart`, `/resume`, `/retry`, `/rollback`, `/save`, `/sessions`, `/sethome`, `/skill`, `/start`, `/status`, `/steer`, `/stop`, `/subgoal`, `/suggestions`, `/thread`, `/title`, `/topic`, `/topup`, `/undo`, `/update`, `/usage`, `/version`, `/voice`, `/whoami`, `/yolo`.

## Kept

`/absence`, `/attack`, `/checkin`, `/complain`, `/dashboard`, `/poll`, `/remind`, `/shield`, `/sum`, `/summit`, `/time`, `/wiki-admin`.

Registration replaces Kella's guild commands with implemented commands and clears its old global commands. Application and attendance records are preserved. Kella persona is untouched.
